---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('projects', ({ data }) => {
    return data.draft !== true;
}); 

const sortedProjects = projects.sort((a, b) =>
    b.data.date.getTime() - a.data.date.getTime()
);
---

<Layout title="Projects - Broderick">
    <main class="container">
        <header class="hero">
            <h1>Projects</h1>
            <p class="subtitle">Here is the place where I keep all of my projects. Some of these are just for showcase, and others are case studies or tools that you can use for yourself.
                <br><br>

            </p>

        </header>

        <nav class="nav">
            <div class="nav-menu">
                <a href="/" class="nav-link nav-triangle">
                    <img src="/assets/svg/triangle.svg" alt="Home">
                </a>
                <a href="/about" class="nav-link">About</a>
                <a href="/projects" class="nav-link active">Projects</a>
                <a href="/thoughts" class="nav-link">Thoughts</a>
                <a href="/experiments" class="nav-link">Experiments</a>
            </div>
        </nav>

        <section class="projects-list">
            {sortedProjects.map((project) => (
                <article class="project-card">
                    <a href={`/${project.slug}`}>
                          <h2>{project.data.title}</h2>
                          <p class="project-description">{project.data.description}</p>
                          <time datetime={project.data.date.toISOString()}>
                              {project.data.date.toLocaleDateString('en-US', {
                                  year: 'numeric',
                                  month: 'long',
                                  day: 'numeric',
                                  timeZone: 'UTC'
                              })}
                          </time>
                      </a>
                  </article>
              ))}
        </section>

    </main>
</Layout>

<style> 
    .projects-list {
        max-width: 800px;
        margin: 2rem 0;
    }

    .project-card {
        padding: 1.5rem 0;
        border-bottom: 1px solid rgba(0,0,0,0.1);
    }

    .project-card:last-child {
        border-bottom: none;
    }

    .project-card a {
        text-decoration: none;
        color: var(--text);
        display: block;
        transition: opacity 0.3s ease;
    }

    .project-card a:hover {
        opacity: 0.7;
    }

    .project-card h2 {
        font-size: 1.8rem;
        margin-bottom: 0.5rem;
        font-weight: 600;
        font-family: 'Instrument Serif', serif;
        font-style: italic;
    }

    .project-description {
        opacity: 0.7;
        margin-bottom: 0.5rem;
        line-height: 1.5;
    }

    time {
        font-size: 0.875rem;
        opacity: 0.5;
    }
</style>